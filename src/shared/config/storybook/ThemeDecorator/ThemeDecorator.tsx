import '../../../../app/styles/index.scss';

// eslint-disable-next-line react/display-name
// export const ThemeDecorator: Decorator = (theme: Theme) => (Story) => (
//     <div className={`app ${theme}`}>
//         <Story/>
//     </div>
// );
